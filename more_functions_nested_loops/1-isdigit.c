#include "main.h"

/**
  * _islower - a function that checks for lowercase character
  *
  * @c: is a letter, lowercase or uppercase
  *
  * Return: On 0 or 1
 */

int _isdigit(int c)
{

	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	return (0);
}
